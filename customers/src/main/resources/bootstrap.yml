spring:
  kafka:
    bootstrap-servers: kafka:9092
    consumer:
      group-id: ${random.value}
      pollTimeout: 3000
      concurrency: 1
    self:
      instanceId:  customers_1
      groupId: customers
      topic: customersTopic
  cloud:
    consul:
      host: 80.78.249.80
      port: 8500
      discovery:
        instanceId: ${spring.application.name}:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}
        healthCheckPath: ${management.endpoints.web.base-path}/health
        healthCheckInterval: 15s
        tags: urlprefix-/home, urlprefix-/discoveryclient
      config:
        format: FILES
        prefix: configuration/master
        enabled: true
        failFast: false
    vault:
      host: 80.78.249.80
      port: 8200
      scheme: http
      connection-timeout: 5000
      read-timeout: 5000
      config:
        order: -10
      token: 54c4b9cf-c37a-8784-a465-3bab828549dd
      consul:
        enabled: true
        role: readonly
        backend: consul
        token-property: spring.cloud.consul.token
  application:
    name: customers
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream, info, health
      base-path: /actuator

