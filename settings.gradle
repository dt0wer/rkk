pluginManagement {
    repositories {
        maven {
            url "${GRADLE_PLUGINS_URL}"
        }
    }
}

rootProject.name = 'rkk'
include ':applications'
include ':customers'
include ':kafka-commons'